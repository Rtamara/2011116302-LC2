@model IEnumerable<_2011116302_ENT.Entityes.AdministradorLinea>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.numeroLinea)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.modalidad)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.Estado)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.numeroLinea)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.modalidad)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.Estado)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.AdministradorLineaId }) |
            @Html.ActionLink("Details", "Details", new { id=item.AdministradorLineaId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.AdministradorLineaId })
        </td>
    </tr>
}

</table>
