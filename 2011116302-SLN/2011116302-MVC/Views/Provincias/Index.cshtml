@model IEnumerable<_2011116302_ENT.Entityes.Provincia>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.Departamento.CodigoDepartamento)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.CodigoProvincia)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.NombreProvincia)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.Departamento.CodigoDepartamento)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.CodigoProvincia)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.NombreProvincia)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.ProvinciaId }) |
            @Html.ActionLink("Details", "Details", new { id=item.ProvinciaId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.ProvinciaId })
        </td>
    </tr>
}

</table>
