@model IEnumerable<_2011116302_ENT.Entityes.LineaTelefonica>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.AdministradorLinea.numeroLinea)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.numeroTelefonico)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.iccid)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.fechaActivacion)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.AdministradorLinea.numeroLinea)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.numeroTelefonico)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.iccid)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.fechaActivacion)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.LineaTelefonicaId }) |
            @Html.ActionLink("Details", "Details", new { id=item.LineaTelefonicaId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.LineaTelefonicaId })
        </td>
    </tr>
}

</table>
