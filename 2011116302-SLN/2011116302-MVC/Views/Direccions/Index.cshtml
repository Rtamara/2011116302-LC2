@model IEnumerable<_2011116302_ENT.Entityes.Direccion>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model._Ubigeo.CodigoUbigeo)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.DescripcionDireccion)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item._Ubigeo.CodigoUbigeo)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.DescripcionDireccion)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.DireccionId }) |
            @Html.ActionLink("Details", "Details", new { id=item.DireccionId }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.DireccionId })
        </td>
    </tr>
}

</table>
